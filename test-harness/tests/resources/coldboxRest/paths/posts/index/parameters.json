{
    "parameters": [
        {
            "name": "pageSize",
            "description": "The number of items per page",
            "in": "query",
            "required": false,
            "schema": {
                "type": "integer",
                "default": 25
            }
        },
        {
            "name": "pageNumber",
            "description": "The page of items to return",
            "in": "query",
            "required": false,
            "schema": {
                "type": "integer",
                "default": 1
            }
        }
    ]
}